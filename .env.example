# Supabase — get these from Project Settings -> API
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
# Server-only secret (NEVER expose to the client). Settings -> API -> service_role
SUPABASE_SERVICE_ROLE_KEY=
